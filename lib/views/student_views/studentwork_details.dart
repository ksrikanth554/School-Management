import 'package:flutter/material.dart';

class StudentWorkDetails extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Welcome To Student Work Details'),),
    );
  }
}